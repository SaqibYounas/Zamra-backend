version: '3.8'

services:
  zamra-backend:
    build:
      context: .       
      dockerfile: Dockerfile
    container_name: zamra-backend-app
    restart: always     
    ports:
      - "5000:3001"    
    env_file:
      - .env           
    environment:
      - NODE_ENV=production